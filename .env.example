# PayFlow Configuration
PAYFLOW_MODE=local        # 'local' for simulation, 'live' for real processing
PORT=3001

# Stripe (optional — test keys work in local mode)
STRIPE_SECRET_KEY=sk_test_placeholder

# PayPal (optional — test keys work in local mode)
PAYPAL_CLIENT_ID=test
PAYPAL_SECRET=test
